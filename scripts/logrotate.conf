# Docker container log rotation configuration
# Copy to /etc/logrotate.d/moviesdb on production host
/var/lib/docker/containers/*/*.log {
    rotate 7
    daily
    compress
    missingok
    notifempty
    copytruncate
    maxsize 50M
}
